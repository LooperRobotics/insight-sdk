# Insight 9 SDK Windows 版用户指南

## 1. 概述
Insight 9 SDK Windows 版是一个静态库，用于从三路 UVC 相机（主 MJPEG、左/右 Mono8）和两路 HID 设备（IMU、VIO）采集数据，并通过 UVC 扩展单元（Extension Unit）提供相机参数（曝光、增益、亮度、白平衡等）的控制能力。

SDK 基于 DirectShow 和 FFmpeg 实现视频捕获，通过 HIDAPI 读取 IMU/VIO 数据，利用 IKsControl 访问 UVC 扩展单元。设备发现使用指定的 VID/PID（默认 0x1d6b / 0x0104），自动排序并选择三个视频设备和两个 HID 设备。捕获的图像和传感器数据通过回调函数实时传递给应用程序。
（！注：FFmpeg 预编译库为修改版本，包含了对深度相机的特殊处理，确保 Z16 格式正确识别和解码。若使用官方 FFmpeg 预编译版本，可能无法正确处理深度数据。处理代码已经开源，可在 GitHub 上找到。）

## 2. 特性
自动设备发现：根据 VID=0x1d6b、PID=0x0104 自动查找 UVC 和 HID 设备。

智能选择：

UVC：枚举所有匹配的视频节点，按路径字符串排序，取前三个作为 RGB、灰度、深度相机（初始化失败如果少于3个设备）。

HID：枚举匹配的 HID 设备，按接口号区分 IMU（接口0）和 VIO（接口1）。

多线程采集：每个设备独立线程，互不阻塞。

回调机制：图像、IMU、VIO 数据实时回调给应用程序。

扩展单元（XU）参数控制：完整控制相机参数，包括曝光、增益、亮度、对比度、伽马、色调、饱和度、锐度、白平衡等。

简洁 API：仅需少量调用即可启动/停止数据采集和调整相机参数。

## 3. 依赖项
操作系统：Windows 7 / 8 / 10 / 11（x64）

构建工具：Visual Studio 2019 或更高版本，CMake 3.20+

运行时：

FFmpeg 预编译库（shared 版本，包含 avcodec, avformat, avdevice, avutil, swscale, swresample）

HIDAPI 预编译库（Windows 版，包含 hidapi.dll 和 hidapi.lib）

权限：运行示例需要管理员权限（访问 UVC 扩展单元需要）

## 4. 构建与安装
### 4.1 安装编译环境
安装 Visual Studio 2019 或更高版本，确保安装了“使用 C++ 的桌面开发”工作负载。
安装 CMake 3.20 或更高版本，并将其添加到系统 PATH 中。
具体版本要求可能会随时间更新，请参考 SDK 的 CMakeLists.txt 中的最低版本要求。
若无法下载，tools中提供了 Visual Studio 2026 和 cmake-4.3.3安装包，可自行下载安装。

### 4.2 准备预编译库
将 FFmpeg 和 HIDAPI 的预编译文件按以下目录结构放置于 SDK 根目录：

text
Insight_SDK_Windows/
├── ffmpeg-z16/
│   ├── include/          # FFmpeg 头文件（libavcodec, libavformat...）
│   ├── lib/              # .lib 文件（avcodec.lib, avformat.lib...）
│   └── bin/              # .dll 文件（avcodec-*.dll, avfilter-*.dll...）
├── hidapi-prebuilt/
│   ├── include/          # hidapi.h
│   ├── lib/              # hidapi.lib
│   └── bin/              # hidapi.dll
├── CMakeLists.txt
├── Insight9SDK.cpp/h
├── ExtensionUnitControl.cpp/hpp
└── example.cpp

### 4.3 使用 CMake 构建
提供 build.bat 脚本，内容如下：

batch
@echo off
setlocal enabledelayedexpansion

echo Building Insight9SDK...

if exist build (
    echo Removing old build directory...
    rmdir /s /q build
)

mkdir build
cd build

cmake .. -G "Visual Studio 18 2026" -A x64 -DUSE_SYSTEM_FFMPEG=OFF -DUSE_SYSTEM_HIDAPI=OFF

cmake --build . --config Release

cd ..
echo.
echo Build completed. Example executable is in build\Release\example.exe

注意：根据你的 Visual Studio 版本调整生成器名称（如提供的 "Visual Studio 18 2026"）。

打开 VS环境 安装的 Developer Command Prompt for VS，cd进入文件根目录下运行 build.bat 即可生成 Insight9SDK.lib 和示例程序 example.exe。

### 4.4 手动构建（可选）
若不想使用 CMake，可使用 Visual Studio 创建静态库项目，添加所有 .cpp 文件，并配置：

附加包含目录：ffmpeg-z16/include，hidapi-prebuilt/include

附加库目录：ffmpeg-z16/lib，hidapi-prebuilt/lib

附加依赖项：avcodec.lib avformat.lib avdevice.lib avutil.lib swscale.lib swresample.lib hidapi.lib ole32.lib strmiids.lib uuid.lib

## 5. API 参考
### 5.1 头文件
c
#include "Insight9SDK.h"

### 5.2 数据结构 camera_params
与 Linux 版本相同，定义如下：

c
typedef struct {
    uint8_t cam_id;             // 相机 ID (0: RGB, 1: Mono)
    uint8_t resolution;         // 分辨率索引（未启用）
    uint8_t frame_rate;         // 帧率索引（未启用）
    float exposure_time;        // 曝光时间（0.0 ~ 0.03 秒）
    float exposure_gain;        // 曝光增益（1.0 ~ 16.0）
    uint8_t auto_exposure;      // 自动曝光（0 或 1）
    float brightness;           // 亮度（0.0 ~ 127.0）
    float contrast;             // 对比度（0.0 ~ 1.9）
    float gamma_dark;           // 暗部伽马（1.0 ~ 4.0）
    float hue;                  // 色调（0.0 ~ 87.0）
    float saturation;           // 饱和度（0.0 ~ 1.999）
    uint8_t sharpness;          // 锐度（1 ~ 255）
    uint8_t auto_white_balance; // 自动白平衡（0 或 1）
    float white_balance;        // 白平衡（1.0 ~ 3.0）
    uint8_t decimation;         // 抽取因子（未启用）
    uint8_t hardware_model;     // 硬件型号（只读）
} camera_params;

### 5.3 回调函数类型
c
typedef void (*image_callback)(int cam_id, uint8_t *data, size_t size,
                               int width, int height, unsigned int format,
                               uint64_t timestamp, uint64_t right_timestamp,
                               void *userdata);

typedef void (*imu_callback)(float ax, float ay, float az,
                             float gx, float gy, float gz,
                             uint64_t timestamp, void *userdata);

typedef void (*vio_callback)(float px, float py, float pz,
                             float qx, float qy, float qz, float qw,
                             uint64_t timestamp, void *userdata);
cam_id：0 = RGB (MJPEG)，1 = 左灰度 (GREY)，2 = 右灰度 (GREY)，3 = 深度 (Z16)

format：使用 V4L2 四字符码（如 'MJPEG'、'GREY'、'Z16 '）

timestamp：设备微秒时间戳

right_timestamp：右图时间戳（立体相机）

### 5.4 SDK 控制函数
函数	说明
int insight9_receive_init(void)	初始化 SDK，扫描设备。成功返回 0，失败返回 -1。
int insight9_receive_start(void)	启动所有采集线程。
void insight9_receive_stop(void)	停止所有采集线程。
void insight9_receive_cleanup(void)	释放所有资源。
const char* insight9_receive_get_video_dev(int cam_id)	获取指定相机的设备路径（DirectShow 路径）。
void insight9_receive_register_image_callback(...)	注册图像回调。
void insight9_receive_register_imu_callback(...)	注册 IMU 回调。
void insight9_receive_register_vio_callback(...)	注册 VIO 回调。
int insight9_receive_set_active_camera(int cam_id)	设置当前活跃相机（用于参数读写）。
int insight9_receive_get_active_camera(int *cam_id)	获取当前活跃相机。
int insight9_receive_set_camera_params(const camera_params *params)	设置当前活跃相机的参数。
int insight9_receive_get_camera_params(camera_params *params)	获取当前活跃相机的参数。
int insight9_receive_set_camera_params_for(int cam_id, const camera_params *params)	设置指定相机的参数。
int insight9_receive_get_camera_params_for(int cam_id, camera_params *params)	获取指定相机的参数。
const char* insight9_receive_get_hardware_type(void)	返回硬件型号字符串。
所有 set 函数内部都会进行范围校验，超出范围的值将导致函数返回 -1 且不发送任何控制命令。

## 6. 使用示例
c
#include "Insight9SDK.h"
#include <stdio.h>
#include <windows.h>

static volatile BOOL keep_running = TRUE;

BOOL WINAPI console_handler(DWORD dwCtrlType) {
    if (dwCtrlType == CTRL_C_EVENT) keep_running = FALSE;
    return TRUE;
}

void my_image_cb(int cam_id, uint8_t *data, size_t size,
                 int w, int h, unsigned int fmt,
                 uint64_t ts, uint64_t ts_right, void *user) {
    printf("IMG[%d] size=%zu, %dx%d, ts=%llu\n", cam_id, size, w, h, ts);
}

int main() {
    SetConsoleCtrlHandler(console_handler, TRUE);

    if (insight9_receive_init() != 0) {
        fprintf(stderr, "SDK init failed\n");
        return -1;
    }

    insight9_receive_register_image_callback(my_image_cb, NULL);

    if (insight9_receive_start() != 0) {
        fprintf(stderr, "SDK start failed\n");
        insight9_receive_cleanup();
        return -1;
    }

    // 设置 RGB 相机参数
    camera_params params = {0};
    params.cam_id = 0;
    params.brightness = 80.0f;
    params.contrast = 1.2f;
    params.exposure_time = 0.015f;
    insight9_receive_set_camera_params_for(0, &params);

    printf("SDK running, press Ctrl+C to stop...\n");
    while (keep_running) Sleep(1000);

    insight9_receive_stop();
    insight9_receive_cleanup();
    return 0;
}
编译运行（使用 CMake 生成的工程）：

打开 Visual Studio 解决方案 build/Insight9SDK.sln

编译 Release 配置

将 ffmpeg-z16/bin/*.dll 和 hidapi-prebuilt/bin/hidapi.dll 复制到 build/Release/

以管理员身份运行 example.exe

或者直接使用 build.bat 自动完成编译和 DLL 复制（需正确配置 CMakeLists.txt 中的自定义命令）。

## 7. 重要说明
数据生命周期：图像回调中的 data 指针指向 SDK 内部缓冲区，会在下一次帧到达时被覆盖。如需保留数据，请在回调中拷贝。

线程安全：回调运行在采集线程中，避免阻塞操作（如文件 I/O、长时间计算）以免丢帧。

权限要求：访问 UVC 扩展单元需要管理员权限，否则 XU 控制函数会失败（但视频采集仍可工作）。

设备数量要求：SDK 要求至少 3 个匹配 VID/PID 的 UVC 设备和 2 个 HID 设备（接口 0 和 1），否则 init 会失败。

热插拔：设备断开后采集线程会自动尝试重连，恢复后数据流继续。

USB 稳定性：反复快速插拔可能导致 USB 栈不稳定，遇到问题时请重启计算机或重新插拔设备。

参数校验：所有参数设置函数都会进行范围校验，拒绝超限值以防止 USB 通信错误。

扩展单元可用性：如果 XU 初始化失败（如驱动不支持），相机参数函数会返回 -1，但视频采集仍正常工作。

## 8. 参数范围参考
参数	最小值	最大值	说明
exposure_time(s)	0.0	0.03	线性映射
exposure_gain	1.0	16.0	线性映射
auto_exposure	0	1	布尔值
brightness	0.0	127.0	线性映射
contrast	0.0	1.9	线性映射
gamma_dark	1.0	4.0	线性映射
hue	0.0	87.0	线性映射
saturation	0.0	1.999	线性映射
sharpness	1	255	线性映射
auto_white_balance	0	1	布尔值
white_balance	1.0	3.0	线性映射

9. 常见问题
Q: 如何验证设备被正确检测？
A: 运行 example.exe 时会打印找到的 UVC 设备路径（已在 findUvcDevices 中输出调试信息）。也可调用 insight9_receive_get_video_dev() 获取设备路径。

Q: 图像数据是 MJPEG，如何解码？
A: 可使用 libjpeg-turbo、Windows Imaging Component (WIC) 或 OpenCV 的 imdecode。灰度数据为 8-bit 原始像素，Z16 为 16-bit 深度数据（小端序）。

Q: 设置相机参数返回 -1 是什么原因？
A: 可能原因：

扩展单元未成功初始化（检查控制台是否有 [XU] 错误）

参数值超出允许范围

设备断开或 XU 繁忙

未以管理员权限运行程序

Q: 运行时提示“找不到 avfilter-11.dll”等 DLL 错误。
A: 请确保 ffmpeg-z16/bin 下的所有 .dll 文件已复制到可执行文件所在目录，或设置 PATH 环境变量包含该目录。CMakeLists.txt 已配置自动复制（需修改通配符列表包含 avfilter-*.dll）。

Q: 是否需要管理员权限？
A: 是的，访问 UVC 扩展单元需要管理员权限。如果不使用 XU 功能（即不调用任何参数控制 API），可以普通用户身份运行，但视频采集和 HID 数据仍可正常工作。

Q: 能否在虚拟机或 WSL 中运行？
A: 本 SDK 依赖 DirectShow 和原生 USB 驱动，建议在原生 Windows 环境运行。在虚拟机或 WSL2 中可能需要 USB 直通且 VID/PID 会被虚拟化，会导致设备匹配失败。

Q: 如何调试 XU 通信问题？
A: 在 ExtensionUnitControl.cpp 中的 query 函数内已添加错误打印（[XU] KsProperty failed...）。可以使用 USB 分析工具（如 USBPcap）或查看 Windows 事件日志。

## 10. 更新日志
v1.1 (2025-06)：Windows 移植版，基于 DirectShow + FFmpeg + HIDAPI 实现，支持 XU 参数控制。

v1.0 (2025-03)：初始 Linux 版本。

如需进一步协助或定制，请联系开发者。